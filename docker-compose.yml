version: '3.9'
services:
  kick-hypertrain:
    build: .
    container_name: kick-hypertrain
    restart: unless-stopped
    ports:
      - "3443:3443"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs
